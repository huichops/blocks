# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'sass', :output => '.' do
  watch %r{^.+\.scss$}
end

guard 'livereload' do
  watch(%r{.+\.rb})
  watch(%r{.+\.(css|js|html)})
end
